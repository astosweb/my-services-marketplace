# Root env for `docker compose` variable substitution (optional).
# Copy to `.env` and adjust secrets for production.
# Production domains: https://gobid.ee (web), https://admin.gobid.ee, https://api.gobid.ee

JWT_SECRET=change-me-to-a-long-random-secret-at-least-32-chars
# Required when NODE_ENV=production; recommended always (do not reuse JWT_SECRET)
UPLOAD_SIGNING_SECRET=another-long-random-secret-at-least-32-chars
CORS_ORIGIN=http://localhost:3001,http://localhost:3002
# Production: CORS_ORIGIN=https://gobid.ee,https://admin.gobid.ee
PASSWORD_RESET_URL=http://localhost:3002/reset-password
# Production: PASSWORD_RESET_URL=https://gobid.ee/reset-password
API_PUBLIC_URL=http://localhost:3000
# Production: API_PUBLIC_URL=https://api.gobid.ee
NEXT_PUBLIC_WEB_URL=http://localhost:3002
NEXT_PUBLIC_SITE_URL=http://localhost:3002
# Production: NEXT_PUBLIC_SITE_URL=https://gobid.ee
SEED_ON_START=true
# Admin/web are served over HTTP in local compose — must be false or browsers drop auth cookies.
COOKIE_SECURE=false
RESEND_API_KEY=
EMAIL_FROM="Gobid <no-reply@gobid.ee>"

# Optional LAN IP for phone / device testing (empty = localhost only).
# Example: LAN_LOCAL_IP_ADDRESS=192.168.1.122
# Then: web at http://<ip>:3002, iOS → http://<ip>:3000 (`pnpm apply:lan` / `pnpm dev:web`)
LAN_LOCAL_IP_ADDRESS=

# DigitalOcean Spaces (required by docker-compose api service)
# Regional API host only — do not include the bucket in the hostname
SPACES_ENDPOINT=https://fra1.digitaloceanspaces.com
SPACES_REGION=fra1
SPACES_BUCKET=gobid
SPACES_ACCESS_KEY_ID=
SPACES_SECRET_ACCESS_KEY=

# Optional Apple Push Notifications (leave unset to skip APNs in local compose)
# APNS_KEY_ID=
# APNS_TEAM_ID=
# APNS_BUNDLE_ID=ee.gobid.app
# APNS_PRIVATE_KEY=
# APNS_PRODUCTION=false

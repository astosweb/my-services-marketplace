/**
 * Admin no longer owns a separate database.
 * Seed marketplace data (including admin@hero.test) via:
 *   pnpm --filter @hero/api db:seed
 */
console.log("Use apps/api prisma seed instead: pnpm --filter @hero/api db:seed");

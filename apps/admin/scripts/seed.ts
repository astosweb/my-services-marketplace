/**
 * Admin no longer owns a separate database.
 * Seed marketplace data (including admin@gobid.test) via:
 *   pnpm --filter @gobid/api db:seed
 */
console.log("Use apps/api prisma seed instead: pnpm --filter @gobid/api db:seed");
